name: Auto-Merge
on:
  pull_request:
  push:
    branches:
      - master

jobs:
  enable-auto-merge:
    uses: zingimmick/.github/.github/workflows/auto-merge.yml@reusable-workflow
    secrets:
      token: ${{ secrets.ACCESS_TOKEN }}

