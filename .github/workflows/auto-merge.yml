name: Auto-Merge
on:
  pull_request_target:
    types: [labeled]

jobs:
  enable-auto-merge:
    uses: zingimmick/.github/.github/workflows/lint.yml@main
    secrets:
      token: "${{ secrets.GITHUB_TOKEN }}"
